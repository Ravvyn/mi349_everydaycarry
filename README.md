# 11ty Boilerplate

Super basic [11ty](https://www.11ty.dev) starter to get you moving in the right direction and gently introduce some core features like **Passthrough File Copy** for handling images and stylesheets, **layouts**, and templating with [Liquid](https://shopify.github.io/liquid/) and [Markdown](https://www.markdownguide.org).

## Quickstart

1. Create a new repository in your own account from this template.
2. Open the new repository in Codespaces or your own development environment.
3. Make sure you have Node.js installed (this will be automatic if using Codespaces).
4. In your terminal type `npx @11ty/eleventy --serve` to build and view the template.

Create an everyday carry website that features three things you won't leave home without.

Each thing should have it's own page, but the full list of things should be available on the homepage.

For each thing, provide an original picture (with a camera or your phone), 
short description of the item, 
and why you can't leave home without it.

The final site must make use of at least one 11ty layout file for the main HTML headers and footers, and one shortcode for reusable content.

Your site should include at least 10 CSS rulesets.

Your site should have a consistent color palette, look and feel across all pages. 
The final site should have a new theme that fits the content, please don't reuse the demo CSS in the starter template.
